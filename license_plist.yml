# LicensePlist configuration file (https://github.com/mono0926/LicensePlist).

exclude:
  - ShowTime        # Development builds only
  - FLEX            # Development builds only
  - TvOSTextViewer  # tvOS builds only